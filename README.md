# Telegram Travel Bot

**Telegram-бот для туристического агентства**
Позволяет пользователю:

- Подбирать туры самостоятельно (выбор страны, месяца, бюджета)
- Консультироваться со специалистом (запись вопросов в базу данных)
- Просматривать информацию об отелях и контактах агентства
- Навигация через кнопки, включая «Назад» и «Завершить»

## 🚀 Функции

1. **Главное меню** с кнопками:
   - 🏖 Подобрать тур самостоятельно
   - 🏨 Отели
   - 💬 Проконсультироваться со специалистом
   - 📞 Контакты

2. **Подбор тура**:
   - Выбор страны, даты поездки и бюджета через кнопки
   - Кнопка «⬅ Назад» для возврата на предыдущий шаг
   - Кнопка «❌ Завершить» для выхода в главное меню
   - FSM используется для пошагового заполнения формы

3. **Консультация со специалистом**:
   - Пользователь вводит вопрос
   - Вопрос сохраняется в базу данных PostgreSQL

## ⚙️ Установка

1. Клонируем репозиторий:

```bash
git clone https://github.com/username/tg_bot_for_travelling_agency.git
cd tg_bot_for_travelling_agency

2. Создаём виртуальное окружение:
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

3. Устанавливаем зависимости:
pip install -r requirements.txt

4. Настраиваем .env или config.py с токеном бота и данными для PostgreSQL:
API_TOKEN = "ваш_токен_бота"
DB_USER = "postgres"
DB_PASSWORD = "пароль"
DB_NAME = "название_бд"
DB_HOST = "localhost"
DB_PORT = 5432

🗂 Структура проекта
travel_bot/
├── bot.py
├── config.py
├── database.py
├── handlers/
│   ├── __init__.py
│   ├── start.py
│   ├── consultation.py
│   └── tour.py
├── keyboards/
│   ├── __init__.py
│   ├── main_menu.py
│   ├── tour_keyboards.py
│   └── constants.py
└── requirements.txt

🛠 Используемые технологии

Python 3.11+

Aiogram 3.x

PostgreSQL

asyncpg

⚡ Запуск
python bot.py
